export const selectSelectedNetwork = (state) => state.network.selectedNetwork;
export const selectFaucetBalance = (state) => state.network.faucetBalance;
export const selectIsLoading = (state) => state.network.isLoading;
